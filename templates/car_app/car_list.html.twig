{% block body %}
<h2>{{title}}</h2>
<ul>
    {% for el in object %}
            <li>
                {{ el.name }}
            </li>
    {% endfor %}
</ul>

{% if paginate %}
    {{ knp_pagination_render(object) }}
{% endif %}

{% endblock %}